--DESTROY ALL PLOT BUT TETEP BISA HARVEST
local Gardens = workspace:WaitForChild("Gardens")

for _, Plot in ipairs(Gardens:GetChildren()) do
    for _, Obj in ipairs(Plot:GetDescendants()) do
        -- Jangan hapus HarvestPart dan semua child-nya
        if Obj.Name == "HarvestPart" then
            continue
        end

        -- Cek apakah object ini berada di dalam HarvestPart
        local IsInsideHarvestPart = false
        local Parent = Obj.Parent

        while Parent and Parent ~= Plot do
            if Parent.Name == "HarvestPart" then
                IsInsideHarvestPart = true
                break
            end
            Parent = Parent.Parent
        end

        if not IsInsideHarvestPart then
            if Obj:IsA("BasePart") then
                Obj:Destroy()
            end
        end
    end
end
